import styled from "styled-components";
import { colors } from "../colors";
import { fontSize } from "../fontSize";

export const StyledPrimaryCard = styled.View`
background-color: ${colors.card_background_color_primary};
padding: 10px;
margin: 20px 0px;
border-radius: 5px;
`;